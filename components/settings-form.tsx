"use client";
import {useActionState} from "react";
import {saveCompanySettings} from "@/features/settings/actions";
export function SettingsForm({initial}:{initial:{name:string;timezone:string;currency:string;language:string}}){const[state,action,pending]=useActionState(saveCompanySettings,{});return <form action={action} className="settings-form"><label>Company name<input name="name" defaultValue={initial.name}/></label><label>Timezone<select name="timezone" defaultValue={initial.timezone}><option>Asia/Dubai</option><option>Asia/Riyadh</option><option>Europe/London</option></select></label><label>Base currency<select name="currency" defaultValue={initial.currency}><option>AED</option><option>SAR</option><option>GBP</option><option>USD</option></select></label><label>Default language<select name="language" defaultValue={initial.language}><option>English</option><option>Arabic</option></select></label>{state.error&&<p className="form-error">{state.error}</p>}{state.saved&&<p className="form-success">Settings saved.</p>}<button className="gold-button" disabled={pending}>{pending?"Saving…":"Save settings"}</button></form>}
