import {NextRequest,NextResponse} from "next/server";
import {verifyToken} from "@/features/auth/token";
const publicRoutes=["/login","/forgot-password"];
export async function middleware(request:NextRequest){const path=request.nextUrl.pathname;const session=await verifyToken(request.cookies.get("morifar_session")?.value);if(publicRoutes.some(route=>path.startsWith(route))){if(session&&path==="/login")return NextResponse.redirect(new URL("/dashboard",request.url));return NextResponse.next()}if(!session)return NextResponse.redirect(new URL(`/login?next=${encodeURIComponent(path)}`,request.url));if((path.startsWith("/settings")||path.startsWith("/ai-command-center")||path.startsWith("/workflow-engine")||path.startsWith("/executive-copilot")||path.startsWith("/operations-intelligence"))&&!(["Super Admin","CEO","COO"] as string[]).includes(session.role))return NextResponse.redirect(new URL("/dashboard?denied=1",request.url));return NextResponse.next()}
export const config={matcher:["/((?!_next/static|_next/image|favicon.ico).*)"]};
